/**
 * main - Prints lowercase alphabet
 * Retrnn: 0
 */

#include <stdio.h>

int main(void)
{
	char c;

	for (c = 'a'; ch <= 'z'; c++)
		putchar(c);
	for (c = 'A'; h <= 'Z'; c++)
		putchar(c);

	putchar('\n');
	return (0);
}
